<Window x:Class="LedgerSync.UI.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LedgerSync.UI"
        mc:Ignorable="d"
        xmlns:viewmodel="clr-namespace:LedgerSyncViewModel;assembly=LedgerSyncViewModel"
        xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
        x:Name="ShellViewWindow"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        WindowState="{Binding ShellModels.SystemState,UpdateSourceTrigger=PropertyChanged}"
        d:DataContext="{d:DesignInstance Type=viewmodel:ShellViewModel}"
        Opacity="0.9"
        Title="LedgerSync"
        Height="969"
        Width="1500"
        Style="{StaticResource WindowStyle}">
    <behaviors:Interaction.Triggers>
        <behaviors:EventTrigger EventName="Loaded">
            <behaviors:InvokeCommandAction Command="{Binding ShellViewLoadCommand,UpdateSourceTrigger=PropertyChanged}"
                                           PassEventArgsToCommand="True"
                                           CommandParameter="{Binding ElementName=ShellViewWindow,UpdateSourceTrigger=PropertyChanged}" />
        </behaviors:EventTrigger>
    </behaviors:Interaction.Triggers>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.05*" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.1115*" />
                <ColumnDefinition />
                <ColumnDefinition Width="150"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <ComboBox
                        ItemsSource="{Binding ShellModels.ObservableCollectionLanguage,UpdateSourceTrigger=PropertyChanged}"
                          SelectedItem="{Binding ShellModels.ItemLanguage,UpdateSourceTrigger=PropertyChanged}"
                          FontSize="30"
                          Foreground="Black"
                          VerticalContentAlignment="Center"
                          HorizontalContentAlignment="Center">
                    <behaviors:Interaction.Triggers>
                        <behaviors:EventTrigger EventName="SelectionChanged">
                            <behaviors:InvokeCommandAction Command="{Binding LanguageSelectionCommand,UpdateSourceTrigger=PropertyChanged}"
                                                           PassEventArgsToCommand="True"/>
                        </behaviors:EventTrigger>
                    </behaviors:Interaction.Triggers>
                </ComboBox>
            </Grid>
            <Grid Grid.Column="2"
                  >

                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0"
                        Content="&#xEF2D;"
                        FontFamily="Segoe MDL2 Assets"
                        FontSize="30"
                        Foreground="Black"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        BorderThickness="0"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        HorizontalContentAlignment="Center"
                        VerticalContentAlignment="Center"
                        Cursor="Hand"
                        Command="{Binding MiniSystemCommand,UpdateSourceTrigger=PropertyChanged}" />
                <Button Grid.Column="1"
                        Content="&#xEF2E;"
                        FontFamily="Segoe MDL2 Assets"
                        FontSize="30"
                        Foreground="Black"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        BorderThickness="0"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        HorizontalContentAlignment="Center"
                        VerticalContentAlignment="Center"
                        Cursor="Hand"
                        Command="{Binding MaxSystemCommand,UpdateSourceTrigger=PropertyChanged}" />
                <Button Grid.Column="2"
                        Content="&#xEF2C;"
                        FontFamily="Segoe MDL2 Assets"
                        FontSize="30"
                        Foreground="Black"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        BorderThickness="0"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        HorizontalContentAlignment="Center"
                        VerticalContentAlignment="Center"
                        Cursor="Hand"
                        Command="{Binding ExitSystemCommand,UpdateSourceTrigger=PropertyChanged}" />

            </Grid>
        </Grid>



        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.15*" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Button Grid.Row="0"
                        Content="{DynamicResource Key/Secret}"
                        Command="{Binding SecretKeyCommand,UpdateSourceTrigger=PropertyChanged}"
                        FontSize="30"
                        Foreground="Black"
                        Background="Transparent"
                        BorderBrush="LightGray"
                        BorderThickness="0.5" />
                <Button Grid.Row="1"
                        Content="{DynamicResource Data}"
                        Command="{Binding TradeDataCommand,UpdateSourceTrigger=PropertyChanged}"
                        FontSize="30"
                        Foreground="Black"
                        Background="Transparent"
                        BorderBrush="LightGray"
                        BorderThickness="0.5" />
                <Button Grid.Row="2"
                        Content="{DynamicResource Synchronize}"
                        Command="{Binding SyncDataLocalCommand,UpdateSourceTrigger=PropertyChanged}"
                        FontSize="30"
                        Foreground="Black"
                        Background="Transparent"
                        BorderBrush="LightGray"
                        BorderThickness="0.5" />
                <Button Grid.Row="3"
                        Content="{DynamicResource Analyze}"
                        Command="{Binding AnalyzeTradeListCommand,UpdateSourceTrigger=PropertyChanged}"
                        FontSize="30"
                        Foreground="Black"
                        Background="Transparent"
                        BorderBrush="LightGray"
                        BorderThickness="0.5" />
                <Button Grid.Row="4"
                        Content="{DynamicResource Export}"
                        Command="{Binding PrintCommand,UpdateSourceTrigger=PropertyChanged}"
                        FontSize="30"
                        Foreground="Black"
                        Background="Transparent"
                        BorderBrush="LightGray"
                        BorderThickness="0.5" />
            </Grid>
            <Grid Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.15*" />
                    <ColumnDefinition Width="0.15*" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition Height="0.05*" />
                </Grid.RowDefinitions>
                <Grid Grid.Column="0"
                      Background="Transparent"
                      Visibility="{Binding ShellModels.CoinVisibility,UpdateSourceTrigger=PropertyChanged}">
                    <Grid.RowDefinitions>
                        <!--<RowDefinition />-->
                        <RowDefinition Height="0.05*" />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <!--<ListBox  Grid.Row="0"
                              Background="Transparent"
                              Visibility="{Binding ShellModels.CoinVisibility,UpdateSourceTrigger=PropertyChanged}"
                              ItemsSource="{Binding ShellModels.ObservableCollectionCoinEntity,UpdateSourceTrigger=PropertyChanged}"
                              HorizontalContentAlignment="Stretch"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="2">
                                    <Button Grid.Column="0"
                                            Content="{Binding Asset,UpdateSourceTrigger=PropertyChanged}"
                                            Command="{Binding DataContext.QueryTradeListSymbolCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding Asset,UpdateSourceTrigger=PropertyChanged}"
                                            FontSize="30"
                                            Foreground="Black"
                                            Background="Transparent"
                                            BorderBrush="Transparent"
                                            BorderThickness="0"
                                            HorizontalAlignment="Stretch"
                                            VerticalAlignment="Stretch"
                                            HorizontalContentAlignment="Left"
                                            VerticalContentAlignment="Center"
                                            Cursor="Hand" />
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>-->
                    <ComboBox Grid.Row="0"
                              ItemsSource="{Binding ShellModels.ObservableCollectionYear,UpdateSourceTrigger=PropertyChanged}"
                              SelectedItem="{Binding ShellModels.ItemYear,UpdateSourceTrigger=PropertyChanged}"
                              FontSize="30"
                              Foreground="Black"
                              VerticalContentAlignment="Center"
                              HorizontalContentAlignment="Center" />
                    <Grid Grid.Row="1">
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <Button Grid.Row="0"
                                Command="{Binding QueryTradeListMonthCommand,UpdateSourceTrigger=PropertyChanged}"
                                CommandParameter="1"
                                Content="{DynamicResource Jan}"
                                FontSize="20"
                                Foreground="Black"
                                Background="Transparent"
                                BorderBrush="LightGray"
                                BorderThickness="0.5" />
                        <Button Grid.Row="1"
                                Command="{Binding QueryTradeListMonthCommand,UpdateSourceTrigger=PropertyChanged}"
                                CommandParameter="2"
                                Content="{DynamicResource Feb}"
                                FontSize="20"
                                Foreground="Black"
                                Background="Transparent"
                                BorderBrush="LightGray"
                                BorderThickness="0.5" />

                        <Button Grid.Row="2"
                                Command="{Binding QueryTradeListMonthCommand,UpdateSourceTrigger=PropertyChanged}"
                                CommandParameter="3"
                                Content="{DynamicResource Mar}"
                                FontSize="20"
                                Foreground="Black"
                                Background="Transparent"
                                BorderBrush="LightGray"
                                BorderThickness="0.5" />
                        <Button Grid.Row="3"
                                Command="{Binding QueryTradeListMonthCommand,UpdateSourceTrigger=PropertyChanged}"
                                CommandParameter="4"
                                Content="{DynamicResource Apr}"
                                FontSize="20"
                                Foreground="Black"
                                Background="Transparent"
                                BorderBrush="LightGray"
                                BorderThickness="0.5" />
                        <Button Grid.Row="4"
                                Command="{Binding QueryTradeListMonthCommand,UpdateSourceTrigger=PropertyChanged}"
                                CommandParameter="5"
                                Content="{DynamicResource May}"
                                FontSize="20"
                                Foreground="Black"
                                Background="Transparent"
                                BorderBrush="LightGray"
                                BorderThickness="0.5" />
                        <Button Grid.Row="5"
                                Command="{Binding QueryTradeListMonthCommand,UpdateSourceTrigger=PropertyChanged}"
                                CommandParameter="6"
                                Content="{DynamicResource Jun}"
                                FontSize="20"
                                Foreground="Black"
                                Background="Transparent"
                                BorderBrush="LightGray"
                                BorderThickness="0.5" />
                        <Button Grid.Row="6"
                                Command="{Binding QueryTradeListMonthCommand,UpdateSourceTrigger=PropertyChanged}"
                                CommandParameter="7"
                                Content="{DynamicResource Jul}"
                                FontSize="20"
                                Foreground="Black"
                                Background="Transparent"
                                BorderBrush="LightGray"
                                BorderThickness="0.5" />
                        <Button Grid.Row="7"
                                Command="{Binding QueryTradeListMonthCommand,UpdateSourceTrigger=PropertyChanged}"
                                CommandParameter="8"
                                Content="{DynamicResource Aug}"
                                FontSize="20"
                                Foreground="Black"
                                Background="Transparent"
                                BorderBrush="LightGray"
                                BorderThickness="0.5" />

                        <Button Grid.Row="8"
                                Command="{Binding QueryTradeListMonthCommand,UpdateSourceTrigger=PropertyChanged}"
                                CommandParameter="9"
                                Content="{DynamicResource Sep}"
                                FontSize="20"
                                Foreground="Black"
                                Background="Transparent"
                                BorderBrush="LightGray"
                                BorderThickness="0.5" />
                        <Button Grid.Row="9"
                                Command="{Binding QueryTradeListMonthCommand,UpdateSourceTrigger=PropertyChanged}"
                                CommandParameter="10"
                                Content="{DynamicResource Oct}"
                                FontSize="20"
                                Foreground="Black"
                                Background="Transparent"
                                BorderBrush="LightGray"
                                BorderThickness="0.5" />
                        <Button Grid.Row="10"
                                Command="{Binding QueryTradeListMonthCommand,UpdateSourceTrigger=PropertyChanged}"
                                CommandParameter="11"
                                Content="{DynamicResource Nov}"
                                FontSize="20"
                                Foreground="Black"
                                Background="Transparent"
                                BorderBrush="LightGray"
                                BorderThickness="0.5" />
                        <Button Grid.Row="11"
                                Command="{Binding QueryTradeListMonthCommand,UpdateSourceTrigger=PropertyChanged}"
                                CommandParameter="12"
                                Content="{DynamicResource Dec}"
                                FontSize="20"
                                Foreground="Black"
                                Background="Transparent"
                                BorderBrush="LightGray"
                                BorderThickness="0.5" />

                    </Grid>
                </Grid>
                <Grid Grid.Column="1"
                      Background="Transparent"
                      Visibility="{Binding ShellModels.CoinVisibility,UpdateSourceTrigger=PropertyChanged}">
                    <!--<Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition Height="0.1*" />
                        <RowDefinition />
                    </Grid.RowDefinitions>-->
                    <ListBox  Grid.Row="0"
                              Background="Transparent"
                              Visibility="{Binding ShellModels.CoinVisibility,UpdateSourceTrigger=PropertyChanged}"
                              ItemsSource="{Binding ShellModels.ObservableCollectionCoinEntity,UpdateSourceTrigger=PropertyChanged}"
                              HorizontalContentAlignment="Stretch"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="2">
                                    <Button Grid.Column="0"
                                            Content="{Binding Asset,UpdateSourceTrigger=PropertyChanged}"
                                            Command="{Binding DataContext.QueryTradeListSymbolCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding Asset,UpdateSourceTrigger=PropertyChanged}"
                                            FontSize="30"
                                            Foreground="Black"
                                            Background="Transparent"
                                            BorderBrush="Transparent"
                                            BorderThickness="0"
                                            HorizontalAlignment="Stretch"
                                            VerticalAlignment="Stretch"
                                            HorizontalContentAlignment="Left"
                                            VerticalContentAlignment="Center"
                                            Cursor="Hand" />
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <!--<ComboBox Grid.Row="1"
                              ItemsSource="{Binding ShellModels.ObservableCollectionYear,UpdateSourceTrigger=PropertyChanged}"
                              SelectedItem="{Binding ShellModels.ItemYear,UpdateSourceTrigger=PropertyChanged}"
                              FontSize="30"
                              Foreground="Black"
                              VerticalContentAlignment="Center"
                              HorizontalContentAlignment="Center" />
                    <Grid Grid.Row="2">
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0"
                                    Command="{Binding QueryTradeListMonthCommand,UpdateSourceTrigger=PropertyChanged}"
                                    CommandParameter="1"
                                    Content="{DynamicResource Jan}"
                                    FontSize="20"
                                    Foreground="Black"
                                    Background="Transparent"
                                    BorderBrush="LightGray"
                                    BorderThickness="0.5" />
                            <Button Grid.Column="1"
                                    Command="{Binding QueryTradeListMonthCommand,UpdateSourceTrigger=PropertyChanged}"
                                    CommandParameter="2"
                                    Content="{DynamicResource Feb}"
                                    FontSize="20"
                                    Foreground="Black"
                                    Background="Transparent"
                                    BorderBrush="LightGray"
                                    BorderThickness="0.5" />
                        </Grid>
                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0"
                                    Command="{Binding QueryTradeListMonthCommand,UpdateSourceTrigger=PropertyChanged}"
                                    CommandParameter="3"
                                    Content="{DynamicResource Mar}"
                                    FontSize="20"
                                    Foreground="Black"
                                    Background="Transparent"
                                    BorderBrush="LightGray"
                                    BorderThickness="0.5" />
                            <Button Grid.Column="1"
                                    Command="{Binding QueryTradeListMonthCommand,UpdateSourceTrigger=PropertyChanged}"
                                    CommandParameter="4"
                                    Content="{DynamicResource Apr}"
                                    FontSize="20"
                                    Foreground="Black"
                                    Background="Transparent"
                                    BorderBrush="LightGray"
                                    BorderThickness="0.5" />
                        </Grid>
                        <Grid Grid.Row="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0"
                                    Command="{Binding QueryTradeListMonthCommand,UpdateSourceTrigger=PropertyChanged}"
                                    CommandParameter="5"
                                    Content="{DynamicResource May}"
                                    FontSize="20"
                                    Foreground="Black"
                                    Background="Transparent"
                                    BorderBrush="LightGray"
                                    BorderThickness="0.5" />
                            <Button Grid.Column="1"
                                    Command="{Binding QueryTradeListMonthCommand,UpdateSourceTrigger=PropertyChanged}"
                                    CommandParameter="6"
                                    Content="{DynamicResource Jun}"
                                    FontSize="20"
                                    Foreground="Black"
                                    Background="Transparent"
                                    BorderBrush="LightGray"
                                    BorderThickness="0.5" />
                        </Grid>
                        <Grid Grid.Row="3">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0"
                                    Command="{Binding QueryTradeListMonthCommand,UpdateSourceTrigger=PropertyChanged}"
                                    CommandParameter="7"
                                    Content="{DynamicResource Jul}"
                                    FontSize="20"
                                    Foreground="Black"
                                    Background="Transparent"
                                    BorderBrush="LightGray"
                                    BorderThickness="0.5" />
                            <Button Grid.Column="1"
                                    Command="{Binding QueryTradeListMonthCommand,UpdateSourceTrigger=PropertyChanged}"
                                    CommandParameter="8"
                                    Content="{DynamicResource Aug}"
                                    FontSize="20"
                                    Foreground="Black"
                                    Background="Transparent"
                                    BorderBrush="LightGray"
                                    BorderThickness="0.5" />
                        </Grid>
                        <Grid Grid.Row="4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0"
                                    Command="{Binding QueryTradeListMonthCommand,UpdateSourceTrigger=PropertyChanged}"
                                    CommandParameter="9"
                                    Content="{DynamicResource Sep}"
                                    FontSize="20"
                                    Foreground="Black"
                                    Background="Transparent"
                                    BorderBrush="LightGray"
                                    BorderThickness="0.5" />
                            <Button Grid.Column="1"
                                    Command="{Binding QueryTradeListMonthCommand,UpdateSourceTrigger=PropertyChanged}"
                                    CommandParameter="10"
                                    Content="{DynamicResource Oct}"
                                    FontSize="20"
                                    Foreground="Black"
                                    Background="Transparent"
                                    BorderBrush="LightGray"
                                    BorderThickness="0.5" />
                        </Grid>
                        <Grid Grid.Row="5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0"
                                    Command="{Binding QueryTradeListMonthCommand,UpdateSourceTrigger=PropertyChanged}"
                                    CommandParameter="11"
                                    Content="{DynamicResource Nov}"
                                    FontSize="20"
                                    Foreground="Black"
                                    Background="Transparent"
                                    BorderBrush="LightGray"
                                    BorderThickness="0.5" />
                            <Button Grid.Column="1"
                                    Command="{Binding QueryTradeListMonthCommand,UpdateSourceTrigger=PropertyChanged}"
                                    CommandParameter="12"
                                    Content="{DynamicResource Dec}"
                                    FontSize="20"
                                    Foreground="Black"
                                    Background="Transparent"
                                    BorderBrush="LightGray"
                                    BorderThickness="0.5" />
                        </Grid>
                    </Grid>-->
                </Grid>
                <Frame Grid.Column="2"
                       Visibility="{Binding ShellModels.CoinVisibility,UpdateSourceTrigger=PropertyChanged}"
                       Source="{Binding ShellModels.NavigationContent,UpdateSourceTrigger=PropertyChanged}"
                       NavigationUIVisibility="Hidden"
                       BorderBrush="Transparent"
                       BorderThickness="0"
                       JournalOwnership="UsesParentJournal" />
                <Frame Grid.Column="0"
                       Grid.ColumnSpan="3"
                       Visibility="{Binding ShellModels.SecretKeyVisibility,UpdateSourceTrigger=PropertyChanged}"
                       Source="{Binding ShellModels.NavigationSecretKey,UpdateSourceTrigger=PropertyChanged}"
                       NavigationUIVisibility="Hidden"
                       BorderBrush="Transparent"
                       BorderThickness="0"
                       JournalOwnership="UsesParentJournal" />
                <Grid Grid.Row="1"
                      Grid.Column="0"
                      Grid.ColumnSpan="3"
                      HorizontalAlignment="Stretch">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0"
                            Content="{DynamicResource Previous Page}"
                            FontSize="30"
                            Foreground="Black"
                            Background="Transparent"
                            BorderBrush="Transparent"
                            BorderThickness="0"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            HorizontalContentAlignment="Center"
                            VerticalContentAlignment="Center"
                            Cursor="Hand"
                            Command="{Binding PreviousContentCommand,UpdateSourceTrigger=PropertyChanged}" />
                    <Grid Grid.Column="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <!--<TextBlock Grid.Column="0"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   FontSize="30"
                                   Foreground="Black"
                                   Text="第" />-->
                        <TextBlock Grid.Column="1"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   FontSize="30"
                                   Foreground="Black"
                                   Text="{Binding ShellModels.CurrentPage,UpdateSourceTrigger=PropertyChanged}" />
                        <!--<TextBlock Grid.Column="2"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   FontSize="30"
                                   Foreground="Black"
                                   Text="页" />-->
                        <TextBlock Grid.Column="3"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   FontSize="30"
                                   Foreground="Black"
                                   Text="/" />
                        <!--<TextBlock Grid.Column="4"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   FontSize="30"
                                   Foreground="Black"
                                   Text="共" />-->
                        <TextBlock Grid.Column="5"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   FontSize="30"
                                   Foreground="Black"
                                   Text="{Binding ShellModels.TotalPage,UpdateSourceTrigger=PropertyChanged}" />
                        <!--<TextBlock Grid.Column="6"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   FontSize="30"
                                   Foreground="Black"
                                   Text="页" />-->
                    </Grid>
                    <Button Grid.Column="2"
                            Content="{DynamicResource Next Page}"
                            FontSize="30"
                            Foreground="Black"
                            Background="Transparent"
                            BorderBrush="Transparent"
                            BorderThickness="0"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            HorizontalContentAlignment="Center"
                            VerticalContentAlignment="Center"
                            Cursor="Hand"
                            Command="{Binding NextContentCommand,UpdateSourceTrigger=PropertyChanged}" />

                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Window>
